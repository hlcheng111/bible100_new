# 雲端上傳排除清單（避免超過 42MB / 單檔限制）
# 專案總體約 5.8 GB，下列資料夾佔絕大部分，上傳時請排除或改用精簡版。

# === 建議「不要上傳」的資料夾（可省約 4.5 GB）===
backups/
data/
disciple_dynamics/disciple_d text/
disciple_dynamics/*.pdf

# === 最大區塊：languages（約 2.5 GB）===
# 若空間不足，可只上傳部分語言或精簡版：
# languages/

# === 單檔過大範例（多數主機單檔限 10–20 MB）===
# data/orig/gb_parsing.json   (~185 MB)
# data/bibles/bible_data_embedded.js  (~56 MB)
# data/cj/*.db, data/cd/*.db  (20–166 MB each)
# disciple_dynamics 內 PDF  (18–109 MB each)

# === 本地開發語言精簡（不影響上傳，僅瀏覽器載入）===
# config/local-languages.json 預設 enabledOnly: cn, en

# === 建議「雲端精簡版」只上傳（通常 < 20 MB）===
# - index.html, index_backup.html, 404.html
# - ai_tools/
# - nav_hub/
# - smart_ministry/
# - church_ministry/
# - school_management/
# - bible_study/
# - qna/  (若主機允許多檔，約 26 MB；否則用 qna_contents_default 等精簡頁)
# - js/, css/, config/
# - 必要時再選 1–2 個語言子目錄（例如 languages/index_cn.html + 中文 landing）
