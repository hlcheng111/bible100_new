{
  "description": "Local dev: limit active language tracks to reduce scan/load. Copy to local-languages.override.json to customize (gitignored).",
  "enabledOnly": ["cn", "en"],
  "optionalOffDisk": ["vi", "id", "ch", "ad", "kh", "lo", "my"],
  "notes": "Move unused language folders to an external drive and symlink if needed; cloud upload policy见 cloud-upload-exclude.txt"
}
