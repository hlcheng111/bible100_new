---
description: Bible100 成品验收以 file:// index_v5 为准；改 modes 必同步 embedded
alwaysApply: true
---

# Bible100 · file:// 成品验收标准

## 唯一验收口径（用户约定）

- **成品是否完成**，以用户在浏览器打开以下地址所见为准： 
  `file:///C:/Users/hlche/.cursor/bible100_new/index.html`（旧书签 `index_v5.html` 仍会转过来）。
- 用户说「干净 / 符合 / 可以」→ 任务才算完成；**不得**仅用 `http://127.0.0.1:8080` 通过就宣告完成。
- HTTP / 上云若与 `file://` 不一致，**以 file:// 为准修到一致**，或明确说明差异原因与修复计划。

## 改 config 必同步 embedded（file:// 依赖）

`file://` 下 `ConfigLoader` 读 **`js/config-embedded.js`**，不 fetch `config/*.json`。

改 `config/modes.json`、`config/modules.json`、`config/languages.json` 等后：

```powershell
node scripts/generate_config_embedded.js
```

并提醒用户 **`file://` 打开 `index_v5.html` 后 Ctrl+F5**（或无痕）验收。

## 汇报格式（迁就小白）

完成任务时写明：

1. 请在 **`file://` …/index_v5.html** 强刷后检查什么（顶栏 2 / 左栏 / 右栏）。
2. 若动到 modes：已跑 `generate_config_embedded.js`（是/否）。
3. 勿擅自大改用户未点名的模块或导航。

## HTTP 用途（次要）

- 本地 HTTP 仅作 **SQLite/WASM、fetch 子页** 等 `file://` 不便时的辅助测试。
- **不是**默认成品验收标准。
