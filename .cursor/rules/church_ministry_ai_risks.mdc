---
description: 教會事工模組 · 降低 AI／Agent 過度修改風險（靜態站保護）
globs: bible100_new/church_ministry/**/*
alwaysApply: false
---

# church_ministry · AI 協作風險控管

## 必守（降低「壞」風險）

1. **維持靜態站**：不改為 React/Vite/Next 除非使用者明確要求；不隨意加 npm 依賴。
2. **保護總站殼**：不修改 `bible100_new/index_v5.html` 做實驗（使用者另有指示除外）。
3. **單次變更範圍**：優先「同一子目錄或同一主題」，避免一次改 50+ 檔；大改前建議請使用者確認或拆 PR。
4. **iframe／相對路徑**：改連結時檢查從 `modules/*` 出發的 `../../` 是否仍正確；側欄需與 `shell_nav.js` / `bible100ShellNav` 一致。
5. **機密**：Supabase／API key 僅 placeholder 或本機 `.env`；不寫入將提交之公開檔。
6. **資料鍵名**：會友／探訪等沿用 `church_data_bridge.js` 與 `CHURCH_MINISTRY_HUB_AUDIT.md`，不發明新鍵名除非同步文件。
7. **雙語標題**：新增或修改頁面時，`<title>` 須含 **中文 · English**（見 `church_ministry/docs/PAGE_TITLE_BILINGUAL.md`）。

## 建議流程

- 改動後在回覆中列出「受影響路徑」與「手動驗收 1 條路徑」（例如 index_v5 → 教會事工 → 側欄一連結）。
