---
description: Bible100 V2.2 預設 v1 — 舊主線 localStorage canonical（Smart Ministry、問卷、AI Lab）
globs: bible100_new/**/*
alwaysApply: false
---

# Cursor 指揮令 v1（舊主線 canonical）

啟用本規則時，視為 **v1 模式**：以瀏覽器 `localStorage` 為唯一 canonical；不主動引入 Sheets 營運主線。

## 模式：localStorage 為唯一 canonical

- 所有 Smart Ministry、問卷、個人化資料以 localStorage 為主。
- 不得自動將 localStorage raw 同步到 Sheets。
- 不得將 L2/L3 敏感資料寫入任何雲端。
- 不得將問卷全文、個人化資料送入 NotebookLM。

## API / Sheets 限制

- 在 v1 模式下，不得新增、修改、刪除任何 Sheets SSOT。
- 不得新增 Apps Script Web App。
- 不得將舊站導覽改為 Sheets 驅動。

## 允許的行為

- 改善前端 UI/UX。
- 改善 localStorage canonical 的結構（需保持相容；見 `docs/DATA_CONTRACT_v0.1.md`）。
- 改善 Smart Ministry 報告渲染。
- 改善 AI Lab（iframe、`postMessage`）。

## 禁止的行為

- 禁止將舊站資料「上雲」。
- 禁止將舊站 canonical 改為 Sheets。
- 禁止將舊站導覽改為 `navigation_map`。
- 禁止將舊站工具列改為 `ai_tools_config`。

## 目標

保持舊站完全可運作，不破壞任何現有 canonical。
