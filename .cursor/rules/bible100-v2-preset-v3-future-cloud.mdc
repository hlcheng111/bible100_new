---
description: Bible100 V2.2 預設 v3 — 未來全雲端（預留；不覆蓋 v1/v2）
globs: bible100_new/**/*
alwaysApply: false
---

# Cursor 指揮令 v3（未來全雲端）

啟用本規則時，視為 **v3 規劃模式**：僅討論／文件化未來上雲；**不**自動改寫現行 v1 或 v2 實作。

## 模式：所有資料最終上雲（未啟用）

- Smart Ministry、問卷、`church_planning` 現階段仍以本機為主；可預留欄位映射與任務編號。
- 任何實際上雲行為須明確標記任務（例如 **M‑009 / M‑010**）並通過審查。

## 限制

- v3 **不得**自動啟用（不得在未授權下批次同步問卷 raw）。
- v3 **不得**覆蓋 v1 canonical（localStorage 主線）。
- v3 **不得**覆蓋 v2 SSOT（Sheets 表頭與 B‑C 契約）。

## 目標

提供未來遷移空間，不影響現階段 v1/v2 的穩定性。
