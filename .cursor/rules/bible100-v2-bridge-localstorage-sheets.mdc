---
description: Bible100 V2.2 localStorage↔Sheets 映射（防雙 SSOT；與 M-008 對齊）
globs: bible100_new/**/*
alwaysApply: false
---

# localStorage ↔ Sheets 映射表（v1 ↔ v2）

僅在 **M‑008／M‑009** 或使用者明示「橋接」時實作。禁止將 raw 問卷或 L2/L3 敘述預設寫入 Sheets。

## Person（localStorage） ↔ Member（Sheets）

| localStorage 概念 | Sheets |
|-------------------|--------|
| `personId` | `members.member_id` |
| 顯示／法定姓名等 | `members.full_name` |
| email | `members.email` |
| lang | `members.lang` |
| roles | `members.roles` |
| phone（若有） | `members.phone`（可選） |

## GiftProfile（localStorage） ↔ planning_kpi（Sheets）

| 來源 | 目標（僅去識別／摘要） |
|------|----------------------|
| `giftProfile.scores` 匯總 | `planning_kpi` 等之量化或分數欄（依 B‑C 定稿） |
| `topGiftIds` / top3 | `planning_kpi.description` 或專用摘要欄（摘要文字） |
| raw 問卷全文 | **禁止** → Sheets |

## Smart Ministry（localStorage） ↔ planning\_\*（Sheets）

| 來源 | 目標 |
|------|------|
| 堂會級／聚合後指標 | `planning_kpi` 等（依 B‑C） |
| `smartMinistry` raw、個人敘述 | **禁止** → Sheets |

## church_planning（localStorage） ↔ planning_pdca（Sheets）

| 來源 | 目標 |
|------|------|
| `pdca.plan` | `planning_pdca.plan` |
| `pdca.do` | `planning_pdca.do`（實作若與 GAS 保留字衝突，以 B‑C 凍結名為準，例如 `do_text`） |
| `pdca.check` | `planning_pdca.check` |
| `pdca.act` | `planning_pdca.act` |
| raw 個人草稿 | **禁止** → Sheets |

## ai_tools（localStorage） ↔ ai_tools_config（Sheets）

| 來源 | 目標 |
|------|------|
| sidebar 折疊、最近開啟等 UI 狀態 | **禁止** → Sheets |
| 經核准的自訂連結／工具列項目 | `ai_tools_config`（白名單） |
