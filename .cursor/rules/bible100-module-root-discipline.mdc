---
description: 模組根目錄紀律 — 禁止 sub/test/node_modules 等；測試只放 repo tests/
globs: church_planning/**,church_ministry/**,bible_study/**,ai_tools/**,school_management/**,docs/governance/**
alwaysApply: false
---

# 模組根目錄紀律（P2）

> 完整版：`docs/governance/MODULE_ROOT_DISCIPLINE.md`

## 禁止在模組根新建

`node_modules/` · `src/` · `dist/` · `public/` · `test/` · `tests/` · `sub/` · `__tests__/` · `coverage/`

## church_planning 新檔歸屬

| 類型 | 目錄 |
|------|------|
| live 工具 | `tools/` + registry |
| 戰情／擴展 | `companion/` |
| 測試 | repo `tests/`（**不是** `church_planning/test/`） |
| legacy | `_archive/` |

## 必跑（改模組根結構後）

```powershell
python tests/test_module_root_discipline.py
python tests/test_church_planning_root_p1.py
```

## Cursor 行為

- 用戶要求加測試 → 只建 `tests/test_*.py`，不在模組根建 `test/`  
- 用戶要求加子功能 → 進白名單子目錄，不建 `sub/`  
- 發現 Vite/npm 殘留 → 移 `_archive/dev_vite_shell_*`，不留在根
