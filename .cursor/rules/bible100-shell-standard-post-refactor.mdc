---
description: 模組重整收口後啟動：index_v5 殼、contentFrame、導覽搜尋與翻譯入口之 Shell 標準與驗收
alwaysApply: false
---

# Shell 標準（模組重整收口後啟動）

## 何時啟用本規則

在 **模組目錄／入口重整** 達到「可凍結邊界」後，再依本規則收斂 **全站殼與導覽**；重整進行中可僅參考，不必強制一次到位。

## 核心約束（與現有規則併用）

1. **`index_v5.html` 右欄** 固定 `name="contentFrame"`；側欄 `target="contentFrame"` 之連結必須能載入正確頁面。  
2. **禁止殼中殼**：與 `bible100-index-shell-sidebar.mdc` 一致——勿把再嵌同名 `contentFrame` 的模組殼塞進總站右欄。  
3. **從 `nav_hub` 等 iframe 導向殼內容區時**：傳給父層的 URL 必須以 **`index_v5.html` 所在目錄（`bible100_new/`）** 為基準解析，**不可**沿用僅在 `nav_hub/` 下才正確的 `../` 路徑字串（見 `docs/BACKLOG_POST_REFACTOR_SHELL.md` **SH-01**）。

## 收口檢查（建議 PR／發版前）

- 執行：`python scripts/check_phase_tool_and_nav_links.py`（導覽／help 相對 `href`）。  
- 手動煙霧：殼內 **工具總覽 → 目錄搜索 →（搜尋）點結果**，確認右欄 URL 正確。  
- 翻譯：`help/translate.html` 在 **`http(s)://` 或 localhost** 下可測；`file://` 僅提示限制。

## 文檔索引

- **工程總表**：`docs/ENGINEERING_MASTER_ROADMAP.md`  
- **里程碑快照**：`PROJECT_MILESTONE_2026-04-29.md`  
- **Shell backlog**：`docs/BACKLOG_POST_REFACTOR_SHELL.md`  
- **同工 HTML 入口**：`help/project-status-hub.html`
