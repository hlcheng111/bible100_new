---
description: 教會規劃 5F 導遊頁型模板；改 index_plan、sidebar_plan、評估／戰情／RACI 等工具頁時必讀
globs:
  - church_planning/index_plan.html
  - church_planning/sidebar_plan.html
  - church_planning/assessment-os-hub.html
  - church_planning/cta-os-war-room.html
  - church_planning/planning/*.html
  - church_planning/css/planning_guide_shared.css
  - tests/test_church_nav_ui_contract.py
alwaysApply: false
---

# 教會規劃 · 導遊頁型模板（5F 長執決策大腦）

## 使命

教會規劃不是 IT 工具超市。所有牧長／長執可見文案須以 **「屬靈分辨旅程 + 導遊」** 為主，底層算法（CTV、registry）可保留在程式與 `<details>` 技術說明，**不得**當主標題。

## 禁止曝露給使用者

- `Phase 3`、`Phase 2` 等開發階段標記
- 按鈕／H1 主標：`CTA-OS`、`CTV`、`registry`、`attachAssessmentToTalent`
- 樓層代號：`5F` 作為頁面主標

## 允許人話對照

| 內部 | 使用者可見 |
|------|------------|
| CTA-OS 戰情室 | **健康雷達戰情室** |
| CTV 向量 | **六維健康圖**（牧養、屬靈、治理、能力、團隊、成形） |
| assessment-os-hub | **健康診斷中心** |
| RACI | **權責反思工作桌**（可保留 RACI 副標） |

## 落地頁 `index_plan.html`

必含四塊：**屬靈前言** → **體量分流** → **角色分流** → **退修會三部曲**（並註明標準次序見左欄「決策導遊五部曲」）。

CRM 橋接必用 `bible100ShellNav` + `sidebar_crm_journey.html`，禁止 `church_ministry/sidebar.html`。

## 工具頁導航（總站殼內必用）

- SSOT：`church_planning/js/planning_tool_registry.js`（14 工具 path）
- 導航：`church_planning/js/planning_nav.js` → `planningOpenContent(event, relPath)` 經 `postMessage navigate` 載入 `church_planning/…`
- **禁止**在 hub／側欄／戰情室快照表使用裸 `href="xxx.html" target="contentFrame"`（iframe 相對路徑會錯位）
- 戰情室快照：`planningOpenByToolId(event, toolId)`，禁止 `href="#"`

## 側欄 `sidebar_plan.html`

順序對齊 `church_ministry/docs/DECISION_MAKER_PATH.md` 第 1 步：

1. RACI → 2. 評估中心 → 3. 健康雷達 → 4. 異象／策略（SWOT 等可折疊）

## 工具頁頂部 · 導遊小盒子（強制）

每個主要規劃工具頁 `<body>` 開頭（或 hero 前）須嵌入：

```html
<link rel="stylesheet" href="../css/planning_guide_shared.css"> <!-- 路徑依目錄調整 -->
<div class="planning-guide-box">
  <h4>💡 導遊：…</h4>
  <p><strong>👥 適合誰：</strong> …</p>
  <p><strong>📅 何時用：</strong> …</p>
  <p><strong>⛪ 體量：</strong> …</p>
  <p><strong>🎯 填完去哪：</strong> …</p>
</div>
```

必答四問；語氣溫暖、非 KPI 打分框架。

## 必改清單（新增工具頁時）

- [ ] 已引用 `planning_guide_shared.css`
- [ ] 頂部有 `planning-guide-box`
- [ ] H1 無 Phase / CTA-OS 主標
- [ ] 「填完去哪」指向下一步（評估 → 戰情 → 異象 或 CRM 執行層）
- [ ] 跑 `python tests/test_church_nav_ui_contract.py`
