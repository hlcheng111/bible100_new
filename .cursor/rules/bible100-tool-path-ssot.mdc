---
description: 教會規劃／CRM／自動化工具路徑 SSOT — 側欄不得自造 URL，新增頁先登記 manifest
globs:
  - church_planning/sidebar*.html
  - church_ministry/sidebar*.html
  - church_planning/js/planning_tool_registry.js
  - js/church_tools_manifest.js
alwaysApply: false
---

# 教會工具路徑 SSOT

## 唯一清單

- 全站：`js/church_tools_manifest.js`（planning + CRM 24 + automation + guide）
- 十四量表細節：`church_planning/js/planning_tool_registry.js`
- 人讀說明：`church_planning/docs/TOOL_PATH_SSOT.md`

## 命名（Batch 2 才整批改名）

| 類型 | 現行 | 目標檔名 |
|------|------|----------|
| 規劃問卷 | `church_planning/*.html` | `church_planning/tools/tool_{id}.html` |
| 步驟導覽 | `guides/guide_step*.html` | 已定型 |
| CRM 工具 | `modules/…` | `church_ministry/crm/crm_{id}.html` |
| CRM 導覽 | `guide_crm_*.html` | 已定型 |
| 自動化 | `modules/tech/*.html` | `auto_{id}.html`（可選） |

## 側欄規則

1. **禁止**在 `sidebar_plan.html`、`sidebar_crm_journey.html` 等新增未在 manifest 登記的 `href`。
2. 規劃量表連結優先 `planningOpenByToolId` + registry，勿硬編新檔名。
3. 跨模組用 `planningOpenRoot` / `bible100ShellNav`，路徑用 repo 根相對（如 `church_ministry/…`）。
4. 暫存／UAT 只放 `church_planning/_archive/`，不得進 sidebar 主流程。

## 改動後測試

```powershell
python tests/test_church_tools_manifest.py
python tests/test_church_nav_ui_contract.py
```

## 上雲

manifest 的 `targetPath` 僅供 Batch 2 改名預留；**未授權不得**打包 Sheets／Apps Script 部署。
