---
description: Bible100 跨模組資料原則（會友主鍵、canonical、禁止雙寫）
globs: "**/*"
alwaysApply: true
---

# Bible100 跨模組資料與命名空間

與 `module_standards.md`（版面／iframe）、`bible100-conventions.mdc`（專案慣例）並列；**改動任何會寫入資料的頁面或腳本時**遵守以下原則。

## 人員主鍵

- 凡牽涉會友／信徒，**優先對齊** `member_id` 與中央會友（`memberSystemData` / `CentralMemberDB`，見 `js/central_member_db.js`）。
- **禁止**各模組自造另一套「永久人員 ID」作為正式來源（遷移期 `legacy_*` 例外須註明）。

## 單一真相（Single source of truth）

- 每個業務領域應有**明定的正式儲存**與**唯一寫入路徑**；避免同一概念寫入兩種 `localStorage` 結構後不同步。
- **禁止**為圖快把同一筆資料複製到兩套結構卻不維護同步。

## Smart Ministry（已落地規格）

- 正式規格：`smart_ministry/docs/SMART_MINISTRY_DATA_RULES.md`。
- Canonical 鍵：`bible100_smart_ministry_main`；API：`js/smart_ministry_canonical_store.js`（`SmartMinistryCanonical`）。
- `talent_id` = `member_id`；人才↔技能、事奉配對須經該 API，**不要**繞過寫入舊的 `smart_ministry_linking` 作為真相。

## 其他模組

- **複製模式、不複製桶名**：可沿用「canonical + 具名 API + 文件」做法，但**使用各自命名空間**（例如 `bible100_school_*`），勿把非事奉資料塞進 `bible100_smart_ministry_main`。
- 跨模組只透過**約定主鍵**（`member_id`）與**明確讀介面**銜接，避免直接解析他模組整包 JSON。

## 版本與備份

- 結構變更時遞增 `schema_version`，並在對應 `docs/*_DATA_RULES.md`（或模組 README）記錄遷移。
- 重要資料應有**匯出／備份**路徑（JSON 匯出或文件化匯出步驟）。

## 快速檢查（Agent／開發者）

- [ ] 是否使用 `member_id` 而非新造人員主鍵？
- [ ] 正式寫入是否只經模組規定的 API / canonical？
- [ ] 是否需更新該模組的 `*_DATA_RULES.md` 或 `IMPLEMENTATION_ORDER.md`？
