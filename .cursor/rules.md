# Bible100 圣经学习平台 - 统一开发规则\n\n## 🎯 项目概述\n这是一个多语言圣经学习平台，包含6种语言版本（CN, EN, VI, ID, CH, AD），支持PC、云端和手机运行。\n\n## 🚀 自动化工作流程\n- **无需等待批示**：AI助手应自动进行代码修改和测试\n- **工程可视化**：每次修改后提供详细的进度报告\n- **自动测试**：修改代码后立即运行测试确保功能正常\n- **进度报告**：每10分钟或长时间操作时提供进度更新\n\n## 📁 项目结构\n```\nbible100_new/\n├── bible_reading/          # 圣经阅读模块（核心）\n├── bible_study/           # 圣经研读模块（高优先级）\n├── ai_tools/              # AI工具模块（中优先级）\n├── church_ministry/       # 教会事工模块（中优先级）\n├── school_management/     # 学校管理模块（低优先级）\n├── smart_ministry/        # 智慧事奉模块\n├── languages/             # 多语言支持（6种语言）\n└── data/                  # 数据库文件（已整理）\n    ├── bibles/           # 圣经版本\n    ├── commentaries/     # 注释书\n    ├── dictionaries/     # 词典\n    ├── crossrefs/        # 串珠\n    ├── devotionals/      # 灵修\n    ├── maps/             # 地图\n    └── lexicons/         # 词汇\n```\n\n## 🏗️ 模块统一标准 (基于语言切换危机经验)\n\n### 📁 标准文件结构\n每个模块必须包含：\n```\nmodule_name/\n├── sidebar.html          # 专门的sidebar文件（必需）\n├── dashboard.html        # 模块主页面（必需）\n├── index.html           # 独立运行页面（可选）\n├── js/                   # JavaScript文件夹\n│   ├── database.js       # 数据库加载器（如需要）\n│   └── [模块功能].js     # 模块特定功能\n├── css/                  # CSS文件夹（可选）\n│   └── style.css         # 模块特定样式\n└── [功能页面].html       # 各功能页面\n```\n\n### 🎨 CSS样式统一标准\n所有sidebar.html必须使用统一的基础样式：\n- **字体**: "Microsoft YaHei", font-size: 10px\n- **背景**: #eef6ff (蓝色主题)\n- **导航颜色**: #004d99 (深蓝)\n- **链接颜色**: #0066cc\n- **边距**: padding: 3px 5px\n\n### 🌐 HTML标准统一\n所有HTML文件必须使用统一标准：\n```html\n<!DOCTYPE html>\n<html lang="zh-CN">\n<head>\n    <meta charset="UTF-8">\n    <meta name="viewport" content="width=device-width, initial-scale=1.0">\n    <title>[模块名称]</title>\n    <!-- 统一的meta标签和基础设置 -->\n</head>\n<body>\n    <!-- 内容 -->\n</body>\n</html>\n```\n\n**禁止使用:**\n- ❌ `<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.01 Transitional//EN">`\n- ❌ FrontPage兼容性代码 `<meta name="Generator" content="Microsoft FrontPage 6.0">`\n- ❌ 过时的HTML4语法\n\n### 💾 数据库访问统一标准\n\n#### 统一数据库加载器\n所有模块必须使用统一的数据库加载方式：\n```javascript\n// 使用统一数据库加载器\nimport { UnifiedDatabaseLoader } from '../bible_reading/js/core/unified-database-loader.js';\n\n// 或者使用全局数据库加载器\nconst dbLoader = new UnifiedDatabaseLoader();\n```\n\n#### 标准数据库路径\n所有数据库文件必须使用统一路径：\n```javascript\n// 统一数据库路径结构\nconst DB_PATHS = {\n    bibles: '../data/bibles/',\n    commentaries: '../data/commentaries/',\n    dictionaries: '../data/dictionaries/',\n    crossrefs: '../data/crossrefs/',\n    devotionals: '../data/devotionals/',\n    maps: '../data/maps/',\n    lexicons: '../data/lexicons/'\n};\n```\n\n#### 数据库文件命名规范\n- **圣经版本**: `[版本名].db` (如: `和合本.db`, `KJV.db`)\n- **注释书**: `[注释名].db` (如: `综合解读.db`, `信望爱注释.db`)\n- **词典**: `[词典名].db` (如: `圣经语汇词典.db`)\n- **串珠**: `[串珠名].db` (如: `信望爱串珠.db`)\n\n#### 统一错误处理\n```javascript\n// 统一的数据库加载错误处理\nasync function loadDatabaseWithFallback(dbPath, fallbackData) {\n    try {\n        // 尝试SQLite\n        const db = await loadSQLiteDatabase(dbPath);\n        return { type: 'sqlite', db: db };\n    } catch (error) {\n        console.warn(`SQLite加载失败，使用JSON备用: ${error.message}`);\n        // 使用JSON备用\n        return { type: 'json', data: fallbackData };\n    }\n}\n```\n\n### 📂 文件路径统一标准\n\n#### 相对路径规范\n所有模块内的文件引用必须使用统一的相对路径：\n```javascript\n// 从模块到根目录的路径\nconst ROOT_PATH = '../';\nconst DATA_PATH = '../data/';\nconst ASSETS_PATH = '../assets/';\nconst JS_PATH = './js/';\nconst CSS_PATH = './css/';\n```\n\n#### 资源引用标准\n```html\n<!-- CSS引用 -->\n<link rel="stylesheet" href="./css/style.css">\n<link rel="stylesheet" href="../css/global.css">\n\n<!-- JavaScript引用 -->\n<script src="./js/module.js"></script>\n<script src="../js/global.js"></script>\n\n<!-- 数据库引用 -->\n<script>\nconst dbPath = '../data/bibles/和合本.db';\n</script>\n```\n\n### 🔧 JavaScript函数命名统一标准\n\n#### 必需的标准函数\n每个sidebar.html必须包含：\n```javascript\n// 1. 菜单切换函数（统一命名）\nfunction toggleCategory(categoryId) {\n    const category = document.getElementById(categoryId);\n    if (category) {\n        category.classList.toggle('active');\n        category.style.display = category.classList.contains('active') ? 'block' : 'none';\n    }\n}\n\n// 2. 初始化函数（统一命名）\nfunction initSidebar() {\n    // 模块特定的初始化代码\n}\n\n// 3. 页面加载完成后执行\ndocument.addEventListener('DOMContentLoaded', function() {\n    initSidebar();\n    // 默认展开某些菜单\n});\n```\n\n#### 禁止的命名方式\n- ❌ `toggleMenu()` - 应使用 `toggleCategory()`\n- ❌ `showHide()` - 应使用 `toggleCategory()`\n- ❌ `menuToggle()` - 应使用 `toggleCategory()`\n\n### 🗃️ 数据存储统一标准\n\n#### 本地存储规范\n```javascript\n// 统一的本地存储键名前缀\nconst STORAGE_PREFIX = 'bible100_';\n\n// 存储用户设置\nlocalStorage.setItem(`${STORAGE_PREFIX}user_preferences`, JSON.stringify(preferences));\n\n// 存储模块状态\nsessionStorage.setItem(`${STORAGE_PREFIX}${moduleName}_state`, JSON.stringify(state));\n```\n\n#### 缓存策略统一\n```javascript\n// 统一的缓存键名和策略\nconst CACHE_CONFIG = {\n    databases: `${STORAGE_PREFIX}db_cache`,\n    userSettings: `${STORAGE_PREFIX}settings`,\n    recentQueries: `${STORAGE_PREFIX}recent_queries`,\n    favorites: `${STORAGE_PREFIX}favorites`\n};\n```\n\n### 特殊主题变体\n- **儿童版 (CH)**: 橙色主题 (#ff6b35, #fff5e6)\n- **高级版 (AD)**: 深色主题 (#1a1a2e, #4fc3f7)\n- **其他模块**: 统一蓝色主题 (#004d99, #eef6ff)\n\n### 🔗 链接和导航标准\n- 所有sidebar链接必须使用 `target="contentFrame"`\n- 返回主页链接使用 `target="_parent"`\n- 包含标准的toggleCategory函数\n\n### 🚫 避免的问题\n1. **CSS冲突**: 不要在sidebar.html中使用全局CSS\n2. **文件混乱**: 不要加载完整页面到sidebarFrame\n3. **JavaScript冲突**: 使用模块特定的函数名\n4. **数据库混乱**: 不要使用不同的数据库加载方式\n5. **路径混乱**: 不要使用绝对路径或不统一的相对路径\n6. **HTML版本混乱**: 不要混用HTML4和HTML5\n7. **存储混乱**: 不要使用不统一的本地存储键名\n\n## 🌐 多语言支持标准\n\n### 语言版本结构\n- **CN**: 中文版 - 使用 `default_sidebar.html`\n- **EN**: 英文版 - 使用 `languages/sidebar_en.html`\n- **VI**: 越南语版 - 使用 `languages/sidebar_vi.html`\n- **ID**: 印尼语版 - 使用 `languages/sidebar_id.html`\n- **CH**: 儿童版 - 使用 `languages/sidebar_ch.html` (橙色主题)\n- **AD**: 高级版 - 使用 `languages/sidebar_ad.html` (深色主题)\n\n### 语言切换机制\n```javascript\nvar langMap = {\n  cn: { page: 'default_sidebar.html', first: 'languages/landP_cn.html' },\n  en: { page: 'languages/sidebar_en.html', first: 'languages/landP_en.html' },\n  vi: { page: 'languages/sidebar_vi.html', first: 'languages/landP_vi.html' },\n  id: { page: 'languages/sidebar_id.html', first: 'languages/landP_id.html' },\n  ch: { page: 'languages/sidebar_ch.html', first: 'languages/landP_ch.html' },\n  ad: { page: 'languages/sidebar_ad.html', first: 'languages/landP_ad.html' }\n};\n```\n\n## 🔧 核心功能要求\n\n### 1. 数据库操作（已修复）\n- ✅ 使用统一数据库加载器 (`unified-database-loader.js`)\n- ✅ 支持SQLite和JSON双重模式\n- ✅ 自动错误处理和备用方案\n- ✅ 综合解读.db释经书模块功能（网站成功标志）\n\n### 2. 文件管理\n- ✅ 已建立Git版本控制\n- ✅ 自动清理无用文件\n- ✅ 保留有效.db文件（33个数据库文件）\n\n### 3. 模块集成标准\n每个模块在主页面index.html中必须有对应的函数：\n```javascript\nfunction openModuleName() {\n  document.getElementById('sidebarFrame').src = 'module_name/sidebar.html?v=' + Date.now();\n  document.getElementById('contentFrame').src = 'module_name/dashboard.html?v=' + Date.now();\n}\n```\n\n## 🎯 关键成功指标\n\n### 命脉功能\n1. **综合解读.db释经书模块** - 网站成功的标志\n2. **数据库操作功能** - 已修复，支持SQLite和JSON\n3. **多版本圣经阅读** - 核心功能\n4. **多语言支持** - 6种语言版本\n\n### 技术指标\n- 数据库文件：33个有效.db文件\n- 支持格式：SQLite + JSON双重模式\n- 兼容性：PC + 云端 + 手机\n- 响应式：移动端适配\n\n## 🛠️ 开发规范\n\n### 代码修改\n- 直接编辑文件，无需等待批示\n- 每次修改后自动运行测试\n- 提供完整的文件路径引用\n- 使用中文响应（用户偏好）\n\n### 测试要求\n- 修改后立即运行测试\n- 显示测试页面完整URL（如 http://localhost/...）\n- 包含实际超链接文本\n- 避免无限循环和重复警告\n\n### 进度报告\n- 每10分钟提供进度更新\n- 长时间操作时报告状态\n- 包含文件夹路径格式：'bible100_new/bible_study/'\n- 指定模块、功能和页面位置\n\n### 🎨 图片和媒体资源统一标准\n\n#### 图片路径规范\n```javascript\n// 统一的图片路径结构\nconst IMAGE_PATHS = {\n    maps: '../data/maps/images/',\n    illustrations: '../assets/images/',\n    icons: '../assets/icons/',\n    backgrounds: '../assets/backgrounds/'\n};\n```\n\n#### 图片命名规范\n- **地图**: `map_[地名]_[语言].jpg` (如: `map_israel_cn.jpg`)\n- **插图**: `illus_[主题]_[编号].jpg` (如: `illus_creation_01.jpg`)\n- **图标**: `icon_[功能].svg` (如: `icon_search.svg`)\n\n### 📱 响应式设计统一标准\n\n#### 断点规范\n```css\n/* 统一的响应式断点 */\n@media screen and (max-width: 768px) {\n    /* 平板样式 */\n}\n\n@media screen and (max-width: 480px) {\n    /* 手机样式 */\n}\n```\n\n### 🔍 搜索功能统一标准\n\n#### 搜索API规范\n```javascript\n// 统一的搜索接口\nclass SearchManager {\n    async searchBibles(query, versions = ['faith']) {\n        // 统一的圣经搜索\n    }\n    \n    async searchCommentaries(query, sources = ['comprehensive']) {\n        // 统一的注释搜索\n    }\n}\n```\n\n### 🌍 国际化统一标准\n\n#### 翻译键名规范\n```javascript\n// 统一的翻译键名结构\nconst TRANSLATION_KEYS = {\n    'common.home': '首页',\n    'common.search': '搜索',\n    'bible.old_testament': '旧约',\n    'bible.new_testament': '新约'\n};\n```\n\n### 🔐 安全和性能统一标准\n\n#### 输入验证规范\n```javascript\n// 统一的输入验证\nfunction validateInput(input, type = 'text') {\n    switch(type) {\n        case 'bible_reference':\n            return /^[1-3]?[a-zA-Z\u4e00-\u9fff]+\s*\d+:\d+(-\d+)?$/.test(input);\n        case 'search_query':\n            return input.length >= 2 && input.length <= 100;\n        default:\n            return input.trim().length > 0;\n    }\n}\n```\n\n### 📊 日志和调试统一标准\n\n#### 日志级别规范\n```javascript\n// 统一的日志管理\nclass Logger {\n    static DEBUG = 0;\n    static INFO = 1;\n    static WARN = 2;\n    static ERROR = 3;\n    \n    static log(level, module, message, data = null) {\n        const timestamp = new Date().toISOString();\n        const prefix = `[${timestamp}] [${module}]`;\n        \n        switch(level) {\n            case Logger.DEBUG:\n                console.debug(`${prefix} DEBUG:`, message, data);\n                break;\n            case Logger.ERROR:\n                console.error(`${prefix} ERROR:`, message, data);\n                break;\n        }\n    }\n}\n```\n\n## ✅ 模块检查清单\n\n### 新模块创建时\n- [ ] 创建了专门的sidebar.html文件\n- [ ] sidebar.html使用了统一的CSS基础样式\n- [ ] 使用HTML5标准（不是HTML4）\n- [ ] 所有链接正确设置了target="contentFrame"\n- [ ] 包含了标准的toggleCategory函数\n- [ ] 使用统一的数据库加载器\n- [ ] 使用统一的文件路径规范\n- [ ] 使用统一的本地存储键名\n- [ ] 在主页面index.html中添加了对应的open函数\n- [ ] 主页面按钮使用了统一样式\n- [ ] 包含响应式设计\n- [ ] 使用统一的日志系统\n\n### 现有模块审查时\n- [ ] 检查是否存在CSS冲突\n- [ ] 确认文件结构符合标准\n- [ ] 验证HTML版本统一（HTML5）\n- [ ] 检查数据库加载方式是否统一\n- [ ] 验证文件路径是否规范\n- [ ] 检查JavaScript函数命名是否统一\n- [ ] 验证链接目标设置正确\n- [ ] 测试菜单切换功能\n- [ ] 确保字体大小统一\n- [ ] 检查响应式设计\n- [ ] 验证国际化支持\n- [ ] 测试搜索功能（如有）\n- [ ] 检查日志输出规范\n\n## 📋 当前状态\n\n### ✅ 已完成\n1. Git版本控制建立\n2. 统一数据库加载器创建\n3. 圣经阅读模块修复\n4. 综合解读释经书模块优化\n5. 数据文件夹清理（33个有效.db文件）\n6. 语言切换危机解决（创建专门sidebar文件）\n7. 模块CSS冲突修复\n8. 字体大小统一标准\n\n### 🔄 进行中\n1. 全站模块标准化审查\n2. 统一开发规范实施\n\n### ⏳ 待完成\n1. AI工具模块优化\n2. 教会事工模块完善\n3. 学校管理模块增强\n4. 多语言内容完善\n\n## 🚨 重要提醒\n\n### 用户偏好\n- 响应语言：中文\n- 工作模式：自动化，无需等待批示\n- 进度报告：每10分钟或长时间操作时\n- 文件引用：提供完整路径\n- 超链接：所有文本和按钮实现为超链接\n\n### 技术限制\n- 避免无限循环\n- 提供状态更新\n- 显示完整URL\n- 包含超链接文本\n- 不使用localhost测试（静态网站）\n\n## 🔒 安全重构和清理程序\n\n### 核心原则\n1. **备份优先**：任何修改前必须先备份\n2. **渐进式修改**：一次只修改一个模块/方面\n3. **功能保护**：绝对不能删除核心功能\n\n### 🛡️ 绝对不能删除的核心文件\n```\n- data/*.db                  # 33个数据库文件\n- index.html                 # 主入口文件\n- default_sidebar.html       # 默认侧边栏\n- languages/sidebar_*.html   # 各语言专门侧边栏\n- unified-database-loader.js # 统一数据库加载器\n```\n\n### 🚦 文件修改安全等级\n- **🟢 安全修改**: CSS样式、非核心HTML、装饰图片\n- **🟡 谨慎修改**: sidebar.html、JavaScript功能文件、配置文件\n- **🔴 禁止修改**: 数据库文件、主入口核心结构、数据库加载器\n\n### 📋 标准重构流程\n1. **备份当前状态** (Git + 文件备份)\n2. **记录功能测试清单** (所有功能测试通过)\n3. **按安全等级修改** (先CSS，再HTML，最后JS)\n4. **每步验证功能** (立即测试，发现问题立即回滚)\n5. **完整验收测试** (所有原功能正常 + 新标准实施)\n\n### ⚠️ 紧急回滚程序\n```bash\n# Git回滚\ngit reset --hard HEAD~1\n\n# 完整备份恢复\nCopy-Item -Path "backup/module_name_timestamp" -Destination "module_name" -Recurse\n```\n\n### 🧹 安全清理分类\n- **🟢 可删除**: .bak/.tmp/.old文件、重复备份、未使用资源\n- **🟡 谨慎删除**: 似乎未使用的CSS/JS、旧版本HTML\n- **🔴 绝对不删**: 任何.db文件、核心配置、多语言文件\n\n## 🎉 成功标准\n1. 综合解读.db释经书模块正常工作\n2. 所有数据库操作稳定运行\n3. 多语言版本完整可用\n4. PC、云端、手机三端兼容\n5. 用户可以独立使用、维护和扩展\n6. 所有模块遵循统一标准，无架构冲突\n7. **重构过程中无功能损失，无不必要阻碍**\n\n## 📖 统一标准参考页面\n\n**完整的开发标准手册已创建：**\n- **文件位置**: `bible100_new/development_standards.html`\n- **访问方式**: 在浏览器中打开该文件即可查看完整的统一标准\n- **包含内容**: 所有模块统一标准、安全重构程序、检查清单等\n- **使用说明**: 开发时随时参考，确保遵循统一标准\n\n---\n**版本**: 2.1  \n**更新日期**: 2025-09-25  \n**状态**: 实施中\n**基于**: 语言切换危机解决经验 + 安全重构程序\n