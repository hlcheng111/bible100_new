# Backups & audit artifacts
backups/
audit_out*.txt
docs/reports/audit_out*.txt

# Build / deps
node_modules/
**/dist/
hymn_management/dist/
church_planning/dist/
church-planning/.next/
__pycache__/
*.pyc

# Local data (backup via scripts/backup_data.ps1 — not synced to GitHub)
data/

# Non-mainline archive (per project rules)
archive/

# Oversized local reference HTML (>100MB — GitHub limit)
doc_viewer/hymnology_practical/

# Large / generated
*.mht
*.zip
*.db
*.mp4
*.pdf
bible100_new_backup_*.zip

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.log

# Local-only config (optional overrides)
config/local-languages.override.json

# Large media (do not sync to GitHub — use local/USB storage)
*.mp3
*.mp4
*.wav
*.ogg
*.jpg
*.jpeg
*.png
*.gif
*.webp
*.bmp
*.tiff
/media/
/audio/
/images/
/img/
/assets/audio/
/assets/images/
# Note: *.svg kept for UI icons in repo; add *.svg here only if you exclude all vectors
