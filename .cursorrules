# Bible100 圣经学习平台 - Cursor AI 工作规则

## 🎯 项目概述
这是一个多语言圣经学习平台，包含6种语言版本（CN, EN, VI, ID, CH, AD），支持PC、云端和手机运行。

## 🚀 自动化工作流程
- **无需等待批示**：AI助手应自动进行代码修改和测试
- **工程可视化**：每次修改后提供详细的进度报告
- **自动测试**：修改代码后立即运行测试确保功能正常
- **进度报告**：每10分钟或长时间操作时提供进度更新

## 📁 项目结构
```
bible100_new/
├── index_v5.html          # 主站壳（双 iframe，本地请用 HTTP 打开）
├── bible_study/           # 圣经研读模块（含原 bible_reading 功能）
├── hymn_management/       # 诗歌管理
├── nav_hub/               # 目录搜索枢纽
├── ai_tools/              # AI工具模块
├── church_ministry/       # 教会事工
├── church_planning/       # 教会规划（Vite React，站内主用）
├── school_management/     # 学校管理
├── languages/             # 多语言教材
├── data/                  # 数据库与解析数据
│   ├── bibles/            # 圣经版本
│   ├── commentaries/      # 注释书
│   ├── cj/                # 综合解读等 SQLite
│   ├── lexicons/          # 词汇（占位，待填充）
│   └── maps/              # 地图（占位，待填充）
└── archive/               # 非主线参考（勿从主导航链入）
```

## 🔧 核心功能要求

### 1. 数据库操作（已修复）
- ✅ 使用统一数据库加载器 (`unified-database-loader.js`)
- ✅ 支持SQLite和JSON双重模式
- ✅ 自动错误处理和备用方案
- ✅ 综合解读.db释经书模块功能（网站成功标志）

### 2. 文件管理
- ✅ 已建立Git版本控制
- ✅ 自动清理无用文件
- ✅ 保留有效.db文件（33个数据库文件）

### 3. 多语言支持
- 支持6种语言：CN, EN, VI, ID, CH, AD
- 所有文本和按钮实现为超链接（用于跟踪）
- 移动端响应式设计

## 🎯 关键成功指标

### 命脉功能
1. **综合解读.db释经书模块** - 网站成功的标志
2. **数据库操作功能** - 已修复，支持SQLite和JSON
3. **多版本圣经阅读** - 核心功能
4. **多语言支持** - 6种语言版本

### 技术指标
- 数据库文件：33个有效.db文件
- 支持格式：SQLite + JSON双重模式
- 兼容性：PC + 云端 + 手机
- 响应式：移动端适配

## 🛠️ 开发规范

### 代码修改
- 直接编辑文件，无需等待批示
- 每次修改后自动运行测试
- 提供完整的文件路径引用
- 使用中文响应（用户偏好）

### 测试要求
- 修改后立即运行测试
- 显示测试页面完整URL（如 http://localhost/...）
- 包含实际超链接文本
- 避免无限循环和重复警告

### 进度报告
- 每10分钟提供进度更新
- 长时间操作时报告状态
- 包含文件夹路径格式：'bible100_new/bible_study/'
- 指定模块、功能和页面位置

## 📋 当前状态

### ✅ 已完成
1. Git版本控制建立
2. 统一数据库加载器创建
3. 圣经研读模块（bible_study，已合并原 bible_reading）
4. 综合解读释经书模块优化
5. 数据文件夹清理（33个有效.db文件）
6. 資料備份：`scripts/backup_data.ps1` + `run_backup_data.bat`，備份 data/ 至 backups/data_YYYYMMDD_HHMMSS/
7. 圣经研读模块数据库修复（bible_study 與 data 路徑調整、綜合解讀資料載入測試）
8. AI工具模块導航與入口優化（ai_tools sidebar、dashboard、總首頁 AI 工具入口）
9. 教会事工 / 学校管理 / 多语言模組入口與導航確認（church_ministry、school_management、languages）

### 🔄 进行中
1. 自动化工作流程建立（修改後的基本檢查清單＋簡短測試報告）
2. Cursor Rules 配置同步（.cursor/rules/* 與本檔案狀態對齊）

### ⏳ 待完成
1. 教会事工模块進階完善（統一管理頁、報表、資料互聯）
2. 学校管理模块進階完善（資料整合、報表與分析）
3. 多语言内容品質提升（翻譯校正、術語統一）
4. AI工具模块進階功能（內容庫、批量工具、自動化工作流）

## 🚨 重要提醒

### Cursor 安全設定
- **在編輯器裡做**：隱私、網路、擴充等安全相關設定請於 Cursor 編輯器設定中自行調整，不寫入本規則檔。

### 用户偏好
- 响应语言：中文
- 工作模式：自动化，无需等待批示
- 进度报告：每10分钟或长时间操作时
- 文件引用：提供完整路径
- 超链接：所有文本和按钮实现为超链接

### 技术限制
- 避免无限循环
- 提供状态更新
- 显示完整URL
- 包含超链接文本

## 🎉 成功标准
1. 综合解读.db释经书模块正常工作
2. 所有数据库操作稳定运行
3. 多语言版本完整可用
4. PC、云端、手机三端兼容
5. 用户可以独立使用、维护和扩展

---
**版本**: 1.0  
**更新日期**: 2025-09-16  
**状态**: 实施中